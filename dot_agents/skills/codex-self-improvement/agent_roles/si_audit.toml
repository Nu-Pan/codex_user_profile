# Complex validation and cross-checking benefit from the flagship reasoning model, especially with sparse handoffs.
model = "gpt-5.4"
model_reasoning_effort = "high"
model_verbosity = "medium"
sandbox_mode = "read-only"
